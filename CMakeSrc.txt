set(LIGHTER_SHADER_SRC
  text.glsl
  ui.glsl
)
set(LIGHTER_DATA_FILES
  ${LIGHTER_SHADER_SRC}
)
set(LIGHTER_SRC
  stdx
  file
  filex
  file.cpp
  mathx
  appx
  oglbase
  ogl
  oclbase
  ocl
  cudabase
  cuda
  cudax
  img
  text
  text.glsl
  ui
  ui.glsl
)
if (LIGHTER_USE_FREEIMAGE)
  set(LIGHTER_SRC
    ${LIGHTER_SRC}
	img.cpp
  )
endif()
