set(LIGHTER_SHADER_SRC
  text.glsl
  ui.glsl
)
set(LIGHTER_DATA_FILES
  ${LIGHTER_SHADER_SRC}
)
set(LIGHTER_SRC
  stdx
  file
  filex
  file.cpp
  mathx
  appx
  oglbase
  ogl
  ogl.cpp
  oclbase
  ocl
  cudabase
  cuda
  cudax
  img
  text
  textx
  text.glsl
  uii
  ui
  uix
  ui.glsl
)
if (LIGHTER_USE_FREEIMAGE)
  set(LIGHTER_SRC
    ${LIGHTER_SRC}
	img.cpp
  )
endif()
if (LIGHTER_USE_FREETYPE)
  set(LIGHTER_SRC
    ${LIGHTER_SRC}
	ui.cpp
  )
endif()