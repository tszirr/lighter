set(LIGHTER_SHADER_SRC
  text.glsl
  ui.glsl
)
set(LIGHTER_DATA_FILES
  ${LIGHTER_SHADER_SRC}
)
set(LIGHTER_SRC
  stdx
  debug
  debug.cpp
  file
  filex
  file.cpp
  mathx
  appx
  input
  oglbase
  ogl
  ogl.cpp
  oclbase
  ocl
  cudabase
  cuda
  cudax
  img
  text
  textx
  text.glsl
  uii
  ui
  uix
  ui.glsl
  scene
  scenex
)
if (LIGHTER_USE_FREEIMAGE)
  list(APPEND LIGHTER_SRC
	img.cpp
  )
endif()
if (LIGHTER_USE_FREETYPE)
  list(APPEND LIGHTER_SRC
	ui.cpp
  )
endif()
if (LIGHTER_USE_SCENE)
  list(APPEND LIGHTER_SRC
	scene.cpp
  )
endif()